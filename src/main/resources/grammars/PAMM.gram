#JSGF V1.0;

grammar pamm;

public <stop_listening> = stop listening;
public <activate> = listen up pam;
public <continuous> = run continuous mode;
public <stop_recording> = stop recording;
public <variable> = create variable step;

/**
* this rule must remain at the bottom of the file
*/
public <command> = [please] (run command) (test);
